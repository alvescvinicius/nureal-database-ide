# JDK 21 ja instalado nesta maquina (nao auto-detectado pelo Gradle, pasta
# fora dos locais padrao que ele varre) — usado como toolchain do modulo (ver
# jvmToolchain(21) no build.gradle.kts), ja que o JDK 24 do JAVA_HOME/PATH
# ainda nao e suportado como TARGET pelo compilador Kotlin.
org.gradle.java.installations.paths=E:/Java/jdk-21_windows-x64_bin/jdk-21.0.8
