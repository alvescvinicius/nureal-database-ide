# Git Rules

Before every implementation:

```bash
git status
git add .
git commit -m "checkpoint: before <feature>"
```

Rules:
- Local commits only.
- Never push automatically.
- Never merge automatically.
- Never rebase automatically.
- Show modified files when finished.
