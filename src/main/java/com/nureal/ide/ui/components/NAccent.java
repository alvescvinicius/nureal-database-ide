package com.nureal.ide.ui.components;

/**
 * Cor semantica de um {@link NCard} (ou de qualquer outro componente que
 * precise de destaque por tipo). {@code NEUTRAL} e o padrao — texto comum,
 * sem cabecalho tintado.
 */
public enum NAccent {
    NEUTRAL,
    INFO,
    WARNING,
    ERROR,
    SQL,
    TOOL,
}
